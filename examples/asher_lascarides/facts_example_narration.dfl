# Max stood up. John greeted him.
-> hasAction(e1, standUp)
-> hasAgent(e1, max)
-> hasAction(e2, greet)
-> hasAgent(e2, john)
# Just like in the Asher-Lascarides paper, pronoun resolution is assumed done by some other magic.
-> hasPatient(e2, max)
